services:
  frontend:
    build: ./Frontend
    ports:
      - "5173:5173"
    env_file:
      - ./frontend/.env
    depends_on:
      - backend

  backend:
    build: ./Backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
