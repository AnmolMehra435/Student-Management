services:
  frontend:
    build: ./Frontend
    container_name: frontend
    ports:
      - "80:80"
    
    depends_on:
      - backend

  backend:
    container_name: backend
    build: ./Backend
    ports:
      - "5000:5000"
    env_file:
      - ./Backend/.env
